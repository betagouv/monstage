.row
  .col-12
    p = internship_offer.description
    p
      span.badge.badge-cyan.px-3.py-2
        - if internship_offer.is_individual?
          | Stage individuel
        - else
          | Stage collectif
    p.text-muted
      = "Semaines : "
      - weeks = current_user.respond_to?(:school) ? internship_offer.internship_offer_weeks.by_weeks(weeks: current_user.school.weeks).ignore_max_candidates_reached(max_candidates: internship_offer.max_candidates).after_current_week : internship_offer.internship_offer_weeks
      = weeks.map(&:human_select_text_method).join(' ou ')
.row.bg-light.py-3.bt-1.bt-light
  .col-12
    h2.h4 Où se déroule ce stage ?
    p = internship_offer.employer_name
    - if internship_offer.employer_website.present?
      p.text-truncate
        = link_to internship_offer.employer_website, target: "_blank", class: 'external' do
          = internship_offer.employer_website
          = " "
          i.fas.fa-external-link-alt
          = "(nouvelle fenêtre)"

    .d-flex
      .circle-font.text-info
        i.fas.fa-map-marker-alt.fa
      p.ml-2
        = internship_offer.street
        br
        = "#{internship_offer.zipcode} #{internship_offer.city}"

- if can? :create, InternshipOffer
  h2.h6.bg-light Contact
  p
    b = internship_offer.tutor_name
    br
    = link_to internship_offer.tutor_phone, "tel:#{internship_offer.tutor_phone}"
    br
    = link_to internship_offer.tutor_email, "email:#{internship_offer.tutor_email}"
