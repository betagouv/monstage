<div class='onboarding-card onboarding-card-sm mb-4' data-controller="signup" data-signup-channel-value="<%= resource.channel %>">
  <header class="header-account">
    <h1 class="h2 text-center">Renvoyer le message d'activation</h1>
  </header>

  <div class="body">
    <%= form_with(model: resource,
                  scope: resource_name,
                  url: confirmation_path(resource_name),
                  local: true,
                  html: { method: :post }) do |f| %>
      <%= render "layouts/form_errors", resource: resource, resource_name: :user %>
      <p>Pour finaliser votre inscription, nous devons d’abord vérifier votre identité. Pour cela indiquez-nous l’adresse électronique associée à votre compte ou bien votre numéro de téléphone.</p>
      <%= f.rg2a_explain_required_asterisk %>
      <%= render 'users/select_channel', label: 'Comment vous contacter ?', form: f, resource: resource, phone: 'SMS' do %>
          <small class="small form-text text-muted">Pour une réponse à un stage, une prise de contact par un employeur ou pour réinitialiser votre mot de passe …</small>
      <% end %>


      <div class="actions d-flex d-sm-block text-center">
        <%= f.submit "Renvoyer", class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>
