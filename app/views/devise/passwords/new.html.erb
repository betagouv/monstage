<div class='onboarding-card onboarding-card-sm mb-4'>
  <header class="header-account">
    <h1 class="h2 text-center mb-0"><%= t('.forgot_your_password') %></h1>
  </header>
  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= f.rg2a_explain_required_asterisk %>
    <%= render "layouts/form_errors", resource: resource, resource_name: :user %>
    <p>Pour changer votre mot de passe, nous devons d’abord vérifier votre identité. Pour cela rappelez-nous l’adresse électronique associée à votre compte.</p>
    <div class="form-group">
      <%= f.label :email do %>
        Adresse électronique (e-mail)
        <%= f.rg2a_required_content_tag %>
      <% end %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control', required: true %>
    </div>

    <div class="actions d-flex d-sm-block text-center">
      <%= f.submit "Envoyer", class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>
<%= render "devise/shared/links" %>
