.form-group
  = form.label :group_id , class: 'fr-label' do
    = "Administration"
  = form.text_field :employer_name,
                      class: 'fr-input',
                      value: current_user.ministry.name,
                      disabled: true
.form-group
  = form.label :employer_name, class: 'fr-label' do
    = "Institution proposant l’offre"
    = form.rg2a_required_content_tag

  = form.text_field :employer_name,
                    class: 'fr-input',
                    placeholder: 'Mon Institution', required: true
= form.hidden_field :is_public, value: true
= form.hidden_field :group_id, value: current_user.ministry_id
