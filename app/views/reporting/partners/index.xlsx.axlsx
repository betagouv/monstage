wb = xlsx_package.workbook

wb.add_worksheet(name: 'Associations') do |sheet|
  # headers
  sheet.add_row(
    ['Association', 'Objectif annuel', 'Date de mise à jour']
  )

  # raws
  @partners.each do |partner|
    sheet.add_row(    
      [partner.name, partner.target_count, partner.updated_at.strftime('%d-%m-%Y')]  
    )
  end
end
