<%= "Bonjour #{@employer.name}," %>

<% if @remindable_application_ids.size.positive? %>
<% -count = @remindable_application_ids.size %>
<%= "Vous avez actuellement #{count} #{"candidature".pluralize(count)} en attente de réponse. En l'absence de réponse de votre part, #{"elle".pluralize(count)} #{count > 1 ? "expireront" : "expirera"} expireront automatiquement dans 30 jours." %>
<% end %>

<%= dashboard_internship_offers_url %>
