.onboarding-card.onboarding-card-lg.mb-4
  header.header-account

  - if !@confirmable_user
    p.alert.alert-danger
      = "Aucun compte n'est lié au mail: #{params[:email]}."
      br
      = "Veuillez "
      = link_to "créer un compte", users_choose_profile_path, class: 'text-white'

  - elsif @confirmable_user.confirmed_at
    p.alert.alert-success
      = "Votre compte est déjà confirmé (#{params[:email]})."
      br
      = "Veuillez vous "
      = link_to "connecter", new_user_session_path(email: params[:email]), class: 'text-white'
  
  - else
    h1.h3.text-blue.mt-4.mb-3.text-center.mb-0 Encore une petite étape...

    p.text-center
      span.confirmation-text= "Votre compte a bien été enregistré "
      strong= "(#{params[:email]})"
      = "."
      br
      | Vous venez de recevoir un mail afin de confirmer votre inscription.

    p.mb-1.test-student-guidelines
      = image_tag asset_pack_path("media/images/onboarding/confirm-desktop.svg"), alt: 'Activer par mail sur ordinateur', class: 'd-none d-md-block', style: "height: auto; width: 100%;"
      = image_tag asset_pack_path("media/images/onboarding/confirm-mobile.svg"), alt: 'Activer par mail sur ordinateur', style: "height: auto; width: 100%;", class: 'd-block d-md-none'
